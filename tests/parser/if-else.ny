kawaii test(x, y) => {
    nani(x == y) {
        yomu("Oui")
    } baka {
        yomu("Non")
    }
}

uWu_nyaa() => {
    a wa ohayo("Enter 0 or 1: ")
    b wa ohayo("Enter 0 or 1: ")
    test(a, b)
}
