kawaii add(x, y) => {
    modoru x mainasu y
}

kawaii sub() => {
    modoru 69
}

kawaii div() => {
    modoru 69
}


uWu_nyaa() => {
    modoru
    modoru 2
    pasu
    a wa b
    # yomu(add(15, 5))
    yomu("hello", "world!")
    # yomu(a purasu b)
    yomu(nai a)
    a wa 32
    modoru
    
    i wa 100
    yomu(i)

    b wa -5 purasu (3 purodakuto 2)
    b wa 4
    b wa ((4 purasu 4))
    c wa nai a
    # a wa nai (a purasu (b mainasu c))
    # a wa -a
    # b wa "a" purodakuto "b"
    # b wa "a" ando "b"
    a++
    a--
    # a wa nai a
    # a wa -a purasu b
    a wa nai "d"
    # a wa nai -a
    # lol(lol())
    yomu("test")
    # yomu(a ando nai b)

    a++
    # a wa (a purasu b)
    ohayo("hello")
    b--
    # yomu(HAI)
    # yomu(IIE)
    # a wa a == b

    name wa ohayo("User: ")
    age wa ohayo()

    daijoubu (hello) {
        a wa b
    }
    modoru

    nani (test) {
        yomu()
        add()
    } nandesuka (nai test) {
        yomu()
    } baka {
        lol()
    }

    ganbatte {
        yomu("TRY")
    }
    gomenasai {
        yomu("ERR")
    }

    daijoubu (HAI) {
        yomu()
    }
}
