uWu_nyaa() => {
   yomu_ln(len(""))
   yomu_ln(len("H"))
   yomu_ln(len("Hello"))
   yomu_ln(len("Hello, world!"))

   arr => {}
   yomu_ln(len(arr))

   arr => [0]
   yomu_ln(len(arr))

   arr => {1, 2, 3}
   yomu_ln(len(arr))

   arr => {1, 2, 3, 4, 5}
   yomu_ln(len(arr))

   arr => [0]
   yomu_ln(len(arr))

   arr => [10]
   yomu_ln(len(arr))
}