uWu_nyaa() => {
    x wa 0
    daijoubu (x < 3) {
        nani (x == 0) {
            nani (x == 0) {
                yamete
            }

            yomu("Don't print inside nani")
        }

        yomu("Don't print inside daijoubu")
    }
}