# Factorial implementation function
kawaii factorial(x) => {
    # if x == 0
    nani(x == 0) {
        modoru 1 # return 1
    }

    # return x * factorial(x - 1)
    modoru x purodakuto factorial(x mainasu 1)
}

# Main program block
uWu_nyaa() => {
    # print result of factorial(5)
    yomu_ln(factorial(5))
}
